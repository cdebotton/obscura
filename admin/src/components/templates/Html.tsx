import React from 'react';

const Html = () => (
  <html lang="en">
    <head>
      <title>Obscura Admin</title>
    </head>
    <body>
      <main id="app" />
    </body>
  </html>
);

export default Html;

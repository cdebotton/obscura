{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "npm-run-all --parallel 'watch:typescript' 'watch:babel'",
    "prewatch:typescript": "mkdir -p .tmp .tmp/es2015-dist",
    "watch:typescript": "tsc --watch --pretty --sourceMap",
    "prewatch:babel": "mkdir -p dist",
    "watch:babel": "babel .tmp/es2015-dist --out-dir ./dist --watch",
    "start:dev": "npm-run-all --parallel 'watch' 'pm2:dev'",
    "pm2:dev": "pm2 start ecosystem.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "npm-run-all": "^4.1.1",
    "typescript": "^2.5.3"
  },
  "dependencies": {
    "@types/koa": "^2.0.39",
    "koa": "^2.3.0",
    "pm2": "^2.7.2"
  }
}

type User {
  id: Int!
  email: String!
  username: String!
  password: String!
}

type Query {
  users: [User]
}

type Mutation {
  createUser(
    email: String!
    username: String!
    password: String!
  ): User!
}